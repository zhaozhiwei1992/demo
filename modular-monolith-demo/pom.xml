<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.acme</groupId>
    <artifactId>modular-monolith-demo</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>
    <name>modular-monolith-demo</name>
    <description>Maven 多模块单体：三层边界守卫 + 可裁剪 + 可拆微服务（配套笔记）</description>

    <properties>
        <revision>1.0.0</revision>
        <java.version>21</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Spring Boot 3.3.x 对应 Spring Modulith 1.2/1.3（1.3.4 实测可用） -->
        <spring.boot.version>3.3.5</spring.boot.version>
        <spring-modulith.version>1.3.4</spring-modulith.version>
        <archunit.version>1.3.0</archunit.version>
    </properties>

    <modules>
        <!-- 依赖版本 BOM -->
        <module>shop-dependencies</module>
        <!-- 框架层（无业务）：common + 各 starter -->
        <module>shop-framework</module>
        <!-- 业务模块（叶子，互不依赖） -->
        <module>shop-module-order</module>
        <module>shop-module-payment</module>
        <!-- 可裁剪演示：注释掉这里 + shop-server 里的依赖，即可整体去掉 user 模块 -->
        <module>shop-module-user</module>
        <!-- 聚合入口（唯一有 main 的工程） -->
        <module>shop-server</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.acme</groupId>
                <artifactId>shop-dependencies</artifactId>
                <version>${revision}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- @RequestParam 等场景需要保留参数名：-parameters -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
