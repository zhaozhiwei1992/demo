#The basename of the resource bundle as well as several other attributes can be configured using the
#spring.messages namespace, as shown in the following example:
spring.messages.basename=messages,i18n.messages
spring.messages.fallback-to-system-locale=false
